# Export variables in your shell; this project does not load .env automatically.
OPENAI_API_KEY=
# Required explicitly; no hardcoded model or pricing assumptions.
OPENAI_MODEL=
# Optional auth for local gateway. If unset, generation endpoint is disabled.
TOKENFLOW_API_KEY=
